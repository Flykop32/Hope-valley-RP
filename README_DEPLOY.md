# Hope Valley RP — Free Website Template

This is a simple static website for a RedM server.

## Quick setup
1) Open `assets/config.js`
2) Paste:
- your Discord invite link
- your `connect ip:port` command
- optional region/tagline

## Deploy options (free)
### GitHub Pages
- Upload this folder to a GitHub repo
- Settings → Pages → choose "Deploy from a branch" → branch `main` → folder `/ (root)`
- Visit the URL GitHub gives you

### Cloudflare Pages
- Create a GitHub repo with these files
- Cloudflare Dashboard → Workers & Pages → Create application → Pages → Connect to Git
- Select the repo
- Build settings: none (static)
- Output folder: `/` (root)

## Customize
- Change the 🤠 emoji in the header to your logo
- Add more pages (businesses, lore, staff, changelog)
- Add images by placing them in `assets/` and referencing them in HTML
